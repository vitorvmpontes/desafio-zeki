# web/

Frontend mínimo: ranking de municípios por risco e tela de detalhe (histórico real vs. previsto, causas dominantes).

**Status:** planejado para o Dia 6 — ver [`docs/DEVLOG.md`](../docs/DEVLOG.md).

## Como vai rodar

```bash
docker compose up -d db api web
```

Detalhes completos (stack escolhida, variáveis de ambiente) serão adicionados aqui conforme o código for implementado.
