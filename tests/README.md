# tests/

Testes automatizados do projeto — principalmente regras de transformação do ETL (cálculo de duração, tratamento de expurgos, join de município) e dos endpoints da API.

**Status:** planejado a partir do Dia 2, crescendo junto com `etl/` e `api/`.
